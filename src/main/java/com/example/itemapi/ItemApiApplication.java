package com.example.itemapi;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main application class for the Item API.
 * This is a Spring Boot application that provides RESTful endpoints for managing items.
 */
@SpringBootApplication
public class ItemApiApplication {

    public static void main(String[] args) {
        SpringApplication.run(ItemApiApplication.class, args);
    }
}
